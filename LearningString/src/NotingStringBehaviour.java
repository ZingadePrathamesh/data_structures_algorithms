public class NotingStringBehaviour {
    public static void main(String[] args) {
        String a = "Hello";
        System.out.println(a + 1);
        Integer b = 56;
        char  c = 'a';
        System.out.println((char)(c+b));

        // + can be used to concatenate strings. The only operator overloading that Java has done.
        // Operator overloading is not possible in Java as it makes the code bad;

    }
}
